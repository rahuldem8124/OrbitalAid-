import ComingSoon from "@/components/ui/ComingSoon";
import { BarChart3 } from "lucide-react";

export default function AnalyticsPage() {
  return (
    <ComingSoon
      icon={BarChart3}
      title="Analytics & Reporting"
      description="Debris growth trends, risk heatmaps, and exportable reports — planned for a later phase, once historical data has accumulated."
    />
  );
}